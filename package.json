{
  "name": "e-commerce",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "cd server & npm run start --prefix server",
    "build": "cd client && npm run build --prefix client",
    "install-client": "cd client && npm cache clean --force --prefix client && rm -rf node_modules --prefix client && npm install --prefix client",
    "heroku-postbuild": "npm run install-client && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": "14.17.5"
  }
}
